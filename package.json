{
	"name": "todo-app-lite",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "pnpm run build-api && pnpm run build-client",
		"build-api": "pnpm --prefix ./api install && pnpm --prefix ./api run build",
		"build-client": "pnpm --prefix ./client install && pnpm --prefix ./client run build"
	},
	"dependencies": {
		"dotenv": "^16.1.4"
	},
	"devDependencies": {
		"typescript": "^5.1.3"
	}
}
