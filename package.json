{
	"name": "todo-app-lite",
	"version": "1.0.0",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"install:client": "yarn --cwd ./client install",
		"install:both": "yarn --cwd ./api install && yarn --cwd ./client install",
		"build:api": "yarn --cwd ./api build",
		"build:client": "yarn --cwd ./client build",
		"build:both": "yarn --cwd ./api build && yarn --cwd ./client build"
	},
	"dependencies": {
		"dotenv": "^16.1.4"
	},
	"devDependencies": {
		"typescript": "^5.1.3"
	}
}
