{
	"name": "todo-app-lite",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "yarn && yarn build-api && yarn build-client",
		"build-api": "yarn --cwd ./api install && yarn --cwd ./api build",
		"build-client": "yarn --cwd ./client install && yarn --cwd ./client build"
	},
	"dependencies": {
		"dotenv": "^16.1.4"
	},
	"devDependencies": {
		"typescript": "^5.1.3"
	}
}
